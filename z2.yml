plugins: ['ci', 'svn']

vcs:
    url: svn://zon/projecten/zicht-libraries/ZichtIter

tasks:
    ci.phpunit:
        pre: composer install
        do: ./vendor/bin/phpunit tests --verbose --coverage-text
